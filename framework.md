> **注意：这是 2026 年初的原始设计草稿，保留作为出处。**
> 实现已经和它有实质差异——候选池流水线（随机候选源、长度带、外围过滤）、
> 接受测试、长度头都是后加的，而"Agent 选 top-degree 作为新边"从未按字面实现过。
> **当前算法以 [ALGORITHM.md](ALGORITHM.md) 为准**，实验结论见 [EXPERIMENTS.md](EXPERIMENTS.md)。

---

你的完整迭代算法流程

你可以直接把这套逻辑写成代码框架：

1.  初始化近邻图 $s_0$。
2.  **For $t = 0$ to $T$ (最大迭代步数):**
3.  过一遍 MLP，获得当前图 $s_t$ 中所有节点的特征 $H$。
4.  **Rule-Picking (Edge Swap)**:
    *   获取 $v$ 的当前 1 跳邻居 $N_1$ 和 2 跳邻居 $N_2$。
    *   Agent 根据概率分布选出top-dregee作为新的边。
5.  **Environment Step (尝试性更新)**:
    *   先对被编辑节点的邻居表做快照，再应用 swap，得到候选图 $s_{t+1}$。
6.  **Reward**:
    *   测算局部性能差异，计算 $r_t = c(s_t) - c(s_{t+1})$。
7.  **RL Update**:
    *   利用 $r_t$ 更新 Rule-Picker 的网络参数（使用 Actor-Critic 或 PPO）。
8.  **Commit (更新步长 $K$)**:
    *   未达到 $K$：按快照回滚到 $s_t$。本步的 swap 只是尝试性的，只用来算奖励、训练网络，图本身不动。
    *   达到 $K$：保留最后一次尝试得到的 $s_{t+1}$ 作为真正的新状态，计数器清零。
    *   $K = 1$ 就是每步都真正更新。$K > 1$ 时同一个 $s_t$ 被采样 $K$ 次，相当于从同一状态出发做 $K$ 次独立 rollout：图的演化慢 $K$ 倍，但策略在每个状态上见到 $K$ 倍的样本。

一个步长内图是完全相同的，所以 $c(s_t)$ 只需测一次、$K$ 次尝试共用，每步仍然只有一次确定性搜索的开销。