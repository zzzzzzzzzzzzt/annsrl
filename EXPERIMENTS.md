# ANN-RL 实验复盘

> **维护约定**：每完成一个实验，在 §5 时间线追加一行（目的 / 做法 / 结果 / 分析 / 引出什么），
> 并同步更新 §4 排行榜、§6 机制链条、§7 下一步猜想。**结论被推翻时不要删除原条目**，
> 改成 `~~划掉~~` 并在下面写 `↳ 更正：…`——本项目已经有 4 次结论翻转，翻转过程本身是证据。
> 最后更新：2026-08-10（Stage 1 完成，TEST 2 通过；Stage 0 的 null 被限定为"条件于线性"）

---

## 1. 命题

在固定检索预算下，用 PPO 学一个"图编辑规则"，使得从**任意** $s_0$ 出发迭代收敛到的图，
比人工构图基线更好。

拆成两个可证伪的子命题：

| | 命题 | 现状 |
|---|---|---|
| **(1)** | $s_0$-无关性：从不同起点收敛到同一个不动点 | **支持**（A2 验证：从 kNN 向上、从重连图向下，都收敛到 ~0.33） |
| **(2)** | 该不动点优于基线 | **失败**，且五轮实验后仍未翻盘 |

命题 (2) 的判据经历过三次抬高，这是本项目最重要的方法论演进：

```
v1  比 s_0 好              → 太弱：随机重连也比 s_0 好，而且好得多
v2  比 kNN + 5% 均匀重连好  → 仍然不够：均匀不是最优的免学习规则
v3  比 C2 长度规则好        → 当前判据（kNN + 5% 边重连到长度分位 65）
```

---

## 2. 实验设置

| 项 | 值 |
|---|---|
| 数据集 | SIFT100K，100k × 128 维，L2，向量归一化 |
| 主操作点 | `dcs_budget=300, ef=32, k=10`，recall@10 |
| 评测查询 | `val_queries[:10000]`，确定性搜索 |
| 训练 | 500 步，`nodes_per_step=512`，`commit_stride=1`，`accept=off`，`--actor_ctx` |
| 统一评分入口 | `eval_harness.py`（**不要**用训练日志里的 recall，见 §3.1） |

### 参考点

| 图 | recall@10 | coverage | ind_max |
|---|---|---|---|
| kNN $s_0$ | 0.2985 | 0.3146 | 199 |
| NSW $s_0$ | 0.6756 | 0.6227 | 71 |

---

## 3. 方法论教训（每一条都曾污染过结论）

这一节放在实验前面，因为**本项目一半的"发现"是测量错误**。

### 3.1 训练日志里的 recall 不可跨 run 比较
每个 run 在自己的 batch 上评测。实测差异：`ctrl_s789` 日志打印 0.8096，统一重评是 0.4997。
→ 一切结论必须过 `eval_harness.py`。

### 3.2 免学习对照是强制的
项目前半程完全没跑随机重连基线。跑出来之后它**赢了** learned policy（0.521 vs 0.339）。
在此之前所有"policy 改进了 $s_0$"的说法都是无意义的——$s_0$ 不是相关的比较对象，
"$s_0$ + 一行 numpy"才是。
→ 固化进 `lib_control.py`，任何 eval 脚本都无法悄悄省掉。

### 3.3 对照必须**按编辑量**匹配，不是按固定剂量
policy 改动 ~409k 条边（全图 17%，是随机重连最优剂量 5% 的 3.4 倍）。
用 5% 对照去比，等于给 policy 3.4 倍的编辑预算优势。
→ `lib_control.rewire(n_edges=...)`。

### 3.4 消融必须多 draw 平均（**曾翻转一个结论的符号**）
同一个消融操作换随机种子：recall sd **0.028**，极差 **0.070**——比本项目**任何**效应都大
（最强的 `--actor_ctx` 才 +0.017）。
第一版 hub 消融是 n=1，给出"枢纽有害 +0.029（3/3）"；平均 5 draw 后变成 **−0.011（0/3）**。
→ `ablate_hub_draws.py` 是模板：≥5 draw，draw seed 跨变体共享（配对）。

噪声还**不是均匀的**：均匀随机对照的 SE(draw) 是 0.0004–0.0013，而针对结构特殊边的变体是
0.0032–0.0135。所以"对照看着稳，臂应该也稳"是反的。

### 3.5 slot 选择是**独立于** draw 的第二个误差分量
"改哪些槽位"和"指向哪里"是两次抽样。同配置换 slot seed 差 0.008–0.014。
这解释了 C2 剂量曲线一开始的假非单调，也意味着**历史上每个 `d(arm−ctrl)` 都少报了 ±0.025**。
→ `eval_harness.py` 的 count-matched 对照现在跨 3 seed 平均并报 SE。

### 3.6 `reachable` 是无界 BFS，饱和在 ~0.994，无信息
真正排序 recall 的是**预算内 coverage**（31%–62%），rho = **+1.0**。训练循环从不记录它。
→ 每次评估都并列报 coverage。

### 3.7 边长必须用**分位**表述，且要小心分位的定义
128 维归一化空间里的裸 L2 没有直觉。全部用"随机点对距离分布的分位"。
**曾犯的错**：把均匀随机目标当成分位 100。它实测是 **46.9**——它就是从那个分布里抽的。
这个错误让"边越长越好"的框架维持了很久，直到 C2 才推翻。

### 3.8 单 seed 会带偏聚合值
`b2_frac010_s42` 比两个同胞低 0.21，把均值从 0.513 拖到 0.443。
`ctrl_s789` 同样形态。→ 识别 regime-shift 的 seed 并单独报告，不要直接平均。

### 3.9 代码 bug 会伪装成自然现象
`rank_neighbourhoods` 把 ±band 偏移加到了窗口**起点**上，实际窗口是 `[r−2band, r]`，单边偏低。
后果是所有重连边系统性变短——而这**恰好长得像**"最长的边没有更长的伙伴可换"这一真实效应，
我一度用缩小 band 去"修"它。是短边的 rank 越界触发 CUDA assert 才暴露的。
→ 任何"看起来符合预期的小偏差"都要单独验证。

### 3.10 功效不足会让"活体解读"给出反向结论
Stage 0 我按 3 draw / 1 slot seed 跑（§3.4 写的是 ≥5 draw，§3.5 写的是 ≥3 slot seed，
都是我图快没照做）。后果：两个名义等价的配置测出 0.5193 和 0.5379，噪声 ±0.02。
更糟的是我在结果逐行到达时做了两次实时推断，**两次都被后来的对照推翻**（见 §5 Stage 0）。
→ 结果没跑完不要下结论；单元格绝对值在 draw 数不足时一律不引用；配对对比可以先看，
因为它抵消共享噪声。

### 3.11 共享基线会让 N 个格子伪装成 N 个独立证据
Stage 0 的 12 个打乱格子全部对同一个 baseline 数相减，而那个数是一次 3-draw 测量。
基线抽低 0.01 ⇒ 12 个 Δ 同时 +0.01 ⇒ "11/12 为正，SE ±0.0025"看起来极强，实则测的是基线。
→ 任何"多个条件 vs 同一个对照"的聚合，SE 必须计入基线的误差，或者干脆改用**双边实测的
配对差**（本项目里 `real − shuffled` 就是对的做法，它把基线消掉了）。

---

## 4. 排行榜（budget=300, ef=32, k=10）

| 图 | recall | 改动边数 | 性质 |
|---|---|---|---|
| kNN $s_0$ | 0.2985 | — | 起点 |
| Tier-1a learned arms | 0.3389 ± 0.0652 | ~409k | 学习 |
| B2 `long_frac 0.01` | 0.502 | 232–315k | 学习 |
| kNN + 5% 均匀重连 | 0.5345 ± 0.0120 | 120k | **一行 numpy** |
| D1 `band` | 0.5309 ± 0.0139 | ~192k | 学习 |
| **D1 `band+periph`** | **0.5403 ± 0.0030** | ~190k | **学习（历史最佳）** |
| **C2 长度规则 @ 5%** | **0.5566 ± 0.0080** | **120k** | **一行 numpy（当前最强）** |
| NSW $s_0$ | 0.6756 | — | 天花板参考 |

**没有任何学习臂在任何操作点上赢过免学习对照。**

---

## 5. 实验时间线

### Tier 0 — 找排序变量

| | |
|---|---|
| **目的** | recall 差异由什么结构量解释？ |
| **做法** | `probe_visits.py`，4 张图（NSW / policy@700 / policy@1100 / kNN），算各结构量与 recall 的秩相关 |
| **结果** | coverage **rho=+1.0**；pops/reached −0.8；**in-degree 只有 −0.4** |
| **分析** | 硬预算下搜索是**成本受限**而非连通性受限。recall 上界是预算耗尽前走过多少不同territory。这直接预言了长边有用、枢纽大小本身没用 |
| **引出** | 放弃入度方向；`reachable` 降级为 liveness 检查 |

### Tier 1a — 入度刹车

| | |
|---|---|
| **目的** | policy 造出 ind_max 8039（NSW 才 71），刹住它是否有用？ |
| **做法** | `--indeg_ctx / --indeg_noop` A/B，8 个 run |
| **结果** | **NULL**。−0.004 ± 0.004（3 个干净 seed），且 3/4 seed 入度反而**升**了 |
| **分析** | 与 Tier 0 的 rho=−0.4 一致。第 4 个 seed 是 regime-shift，混进来会变成 −0.048 |
| **引出** | 关掉 Tier 1b（硬入度上限）、1c |

### 计划外 · 枢纽消融（第一次）

| | |
|---|---|
| **目的** | 枢纽到底是不是致因？ |
| **做法** | `ablate_hub.py`，把 top-N 枢纽的入边重定向到随机点，出度保持 |
| **结果** | ind_max 5225→550，**recall 不变**；剂量匹配的随机对照反而更好 |
| **分析** | 枢纽与 recall 变化**共现但非因果** |
| **引出** | 入度方向彻底关闭（后被 C1b 用逐点精确匹配再次确认） |

### 计划外 · 随机重连基线 ← **本项目的转折点**

| | |
|---|---|
| **目的** | 补上从未跑过的免学习对照 |
| **做法** | `probe_random_rewire.py`，随机挑 f 比例的有向边重指向随机点，保出度 |
| **结果** | **kNN + 5% = 0.521 vs policy 0.339**。剂量响应是倒 U：1%→0.376，2%→0.437，**5%→最优**，20%→0.503，50%→0.330 |
| **分析** | 把 kNN 短边随机化 = 造长边 = NSW 小世界性质的来源。倒 U 说明"保住局部短边 + 少量长边"是结构诉求 |
| **引出** | **判据 v2**。并且把问题从"能否超过起点"改成"能否超过随机长边重连" |

同一个 5% 施加在**最好的学习图**上（0.4997→0.5736）和施加在**裸 kNN** 上（0.2985→0.5750）
**结果一样**。→ 学出来的结构没有提供任何随机长边不能更好提供的东西。

### Tier A — 四个封堵性实验

| 实验 | 目的 | 结果 | 分析 |
|---|---|---|---|
| **A4** 编辑量匹配 | 是不是"选得好但改得太少"？ | policy 改 409k（17%），**匹配后仍输** 0.339 vs 0.495 | 不是量的问题，是**选得比随机差** |
| **A3** budget×ef 扫描 | 有没有某个操作点能翻盘？ | **12/12 格全负**。增益 +0.017 且**与预算无关**（7/7 seed） | 我猜"宽预算会反转"是**错的**，被一个 seed 误导。ef 在固定预算下几乎不起作用 |
| **A2** 从重连图开始训练 | $s_0$-无关性成立吗？ | **−0.158 ± 0.026，3/3 单调下降，到 500 步还在跌** | 命题(1)**成立**、命题(2)**失败**：不动点在 0.33，低于免费基线 |
| **A1** 对照固化 | — | `lib_control.py` + `eval_harness.py` | 见 §3.2 |

**A2 是关键**：policy 从两个方向都收敛到 ~0.33。这把问题从"它会不会收敛"变成
**"为什么不动点在那个位置"**——答案指向 scorer，不指向 RL 机制。

### Tier B — 短边偏好

| 实验 | 目的 | 做法 | 结果 |
|---|---|---|---|
| **B0** | 确认短边偏好存在 | 量 policy 编辑的边长分布 | **确认**。任何起点都落在 add_pct 1.6–2.0；重连图的长边只有 15% 存活（kNN 短边 87%），**5.8× 优先删除** |
| **B1** | 偏好是预训练带来的吗？ | `--scratch` 随机初始化，3 seed | **NULL，且方向相反**。随机初始化 rho(score,len) = **−0.80**，比预训练的 −0.58 **更**偏短 |
| **B2-pre** | 是不是候选池根本没长边？ | 量 2-hop 池自身的长度分布 | **不是**。重连图上池的每节点最长候选到分位 **92.5**，policy 仍然只挑 1.61（0.15× 均匀） |
| **B2** | 把菜单限制成长边 | `--long_frac`，只保留每节点池中最长的 FRAC | **首次打破吸引子**：add_pct 1.67→66，recall 0.299→**0.502**，与随机的差距缩小 15× 到 −0.013 ± 0.025（打平） |

**B1 是最重要的一条**：短边偏好是**架构性**的。dot-product over 平滑 encoder = 天生的
proximity oracle，在**任何**权重下都成立。
→ 这一条直接否掉了"换 loss / 换监督 / 换 encoder（NodeFormer 等 improve.md 路线）"的整条思路。
**唯一剩下的杠杆是动作空间。**

### 枢纽消融（第二次，在长边图上重做）

| | |
|---|---|
| **目的** | 第一次的 null 是在**短边图**上测的，B2 的图 ind_max 5456–7471 且 add_pct 66，结构不同，结论不能免费迁移 |
| **做法** | `ablate_hub_draws.py`：加了**长度匹配对照**（按枢纽边自身的长度十分位分层抽非枢纽边），3 seed × 5 draw |
| **结果** | hub vs 随机对照 **+0.0194 ± 0.0049（3/3）**；hub vs **长度匹配对照 −0.0111 ± 0.0068（0/3）** |
| **分析** | 看起来的"枢纽效应"**全部**是"枢纽入边恰好很长"。长度一匹配，打枢纽反而略差。**枢纽在长边图上依然非因果** |
| **踩坑** | 第一版 n=1，给出符号相反的结论。见 §3.4 |

### C1 — 等长目标重连 ← **第一个正结果**

| | |
|---|---|
| **目的** | 剥离长度后，policy"选了哪个点"这件事本身有没有信息？ |
| **做法** | `c1_reshuffle.py`：把 (u→v) 的 v 换成"在 u 的距离排序中 rank 相差 ±100"的随机点。边长保留到 0.35%，**唯一被销毁的是目标身份** |
| **结果** | | |

| 图 | C1 等长重连 | calib（随机边等长重连） | ref（均匀目标） |
|---|---|---|---|
| policy `b2_frac001` ×3 | **−0.0141 ± 0.0041（0/3）** | −0.0017 ± 0.0006 | +0.0104 ± 0.0064 |
| 学习无关的重连图 ×2（安慰剂） | **−0.0009 ± 0.0006** | −0.0025 ± 0.0009 | +0.0113 ± 0.0103 |
| policy，剔除枢纽入边 | **−0.0096 ± 0.0040（0/3）** | | |

**分析**：这个 2×2 是全部证据。重连**只在 policy 的图上咬得动**，是安慰剂行的 15 倍；
安慰剂干净说明不是操作伪影。policy 行的 SE(draw) 是 0.0000–0.0001，效应被充分分辨。
剔除枢纽后仍然是负的 → **学到的信息住在普通长边上**，枢纽边是另一件事。

**这是本项目第一个同时扛住长度对照和安慰剂的正结果，值 +0.010~0.014。**

### C1b — 拆开：逐点匹配 vs 跨点协同

| | |
|---|---|
| **目的** | C1 同时销毁了"每个点选得准"和"很多点共享同一目标"，得拆 |
| **做法** | `c1b_decompose.py`。`perm`=在枢纽集内置换目标（入度**精确**保持，只换谁用哪个）；`relabel`=把 10 个枢纽映射到 10 个随机点（入度**和共享模式都精确**保持，只换身份） |

| 变体 | Δrecall | 事后 ind_max |
|---|---|---|
| perm 枢纽集内置换 | **+0.0003 ± 0.0059（1/3）** | 7471（精确） |
| relabel 枢纽→随机点 | **+0.0302 ± 0.0118（3/3）** | 7490（精确） |
| uniform 均匀目标 | +0.0288 ± 0.0108（3/3） | 2553 |

**分析**：
- `perm ≈ 0` → policy 没学到"哪个枢纽适合我"，逐点匹配在枢纽上一文不值
- `relabel ≈ uniform` → 共享目标 / 入度集中是**中性**的。注意 relabel 把 ind_max 按住在 7490，
  uniform 掉到 2553，**得分相同**——这是迄今最强的入度 null（逐点精确匹配）
- 剩下的只有**那 10 个点的身份**

**它们错在哪**（`probe_hub_identity.py`）：到质心距离的分位 **99.8 / 99.7 / 98.8**，三 seed 全部。
局部密度反而不一致（0.349 / 0.506 / 0.686 vs 随机 0.568）→ **不是密度，是外围性**。
数据云外围的点是死胡同出口：跳过去花预算，到不了新地方。
而在 `long_frac` 的菜单下，极端外围点恰恰对**最多**源点"看起来最长"，所以 proximity oracle
必然收敛到它们。

### C2 — 长度分位扫描

| | |
|---|---|
| **目的** | 之前的"越长越好"框架被 §3.7 的分位错误推翻了，最优点得实测 |
| **做法** | `c2_lengthsweep.py`。固定随机槽位，只控制一个变量：新目标的**长度分位**（按源点自身的距离排序 rank 实现，jitter ±0.5%） |

| p | 5 | 15 | 30 | 47 | 55 | **65** | 70 | 80 | 92 | 99 |
|---|---|---|---|---|---|---|---|---|---|---|
| recall | .2989 | .3072 | .3436 | .4262 | .5339 | **.5395** | .5234 | .5144 | .5126 | .4740 |

**集中 vs 散开**（3 slot seed × 2 剂量，6/6 全胜）：

| dose | p=65 | uniform（散开） | Δ |
|---|---|---|---|
| 0.02 | 0.5416 ± 0.0102 | 0.4977 ± 0.0088 | **+0.0439 ± 0.0122** |
| 0.05 | **0.5566 ± 0.0080** | 0.5442 ± 0.0139 | +0.0124 ± 0.0060 |

**分析**：最优在**内部**（p≈55–65）。kNN 自己的边在 7.6（太短），policy 的枢纽边在 93.2（太长）。
均匀随机目标只是 p=47 的一个**散开**版本，而集中比散开好 → **长度分布的形状本身有价值**。
→ **判据 v3 = 0.5566 ± 0.0080**。

### D1 — 长度带动作空间

| | |
|---|---|
| **目的** | 把分工按 C1/C2 的证据切开：**规则定长度**（policy 做不到，架构性），**policy 在带内选点**（唯一被证明会做的事） |
| **做法** | `lib/algorithm.py: augment_candidates()`。`--cand_band 55 75`（按分位过滤）+ `--n_rand_cand 256`（追加均匀采样顶点）+ `--max_periph_pct 95`（剔除外围离群点） |

**实现约束（单元检查直接印证）**：

| 配置 | 候选/节点 | 平均分位 | 带内% |
|---|---|---|---|
| 2-hop 原池 | 291.5 | 2.54 | — |
| band 55-75，**不加随机候选** | 2.0 | 7.84 | **0.0** |
| band 55-75，rand 256 | 50.1 | **65.02** | **100.0** |

**2-hop 池里 0% 的候选够得到 55–75 带**（kNN 上池的每节点上限只有分位 8.95）。
所以 `long_frac` 那条路**在原理上**表达不了这个设定，必须加随机候选源。

**结果**：

| 臂 | recall | 跨 seed sd | vs count-matched 随机 |
|---|---|---|---|
| d1_band | 0.5309 | 0.0240 | +0.0003 ± 0.0198（1/3） |
| **d1_periph** | **0.5403** | **0.0052** | **+0.0081 ± 0.0076（2/3）** |

add_pct 每个 run 都是 61.0–62.3（被规则**钉**在带内，不是漂过去的）；ind_max 从 B2 的
5456–7471 降到 2205–4051。

**periph 过滤的真实作用是降方差**：均值 +0.0093 ± 0.0114（2/3，未分辨），
但跨 seed sd 砍了 **4.6 倍**。而且它**不是**靠压枢纽起效（periph 臂 ind_max 并不更低）。

**编辑效率上输了，这是诚实判决**：

| 配置 | 改动边数 | recall |
|---|---|---|
| C2 规则 @5% | 120,000 | **0.5566 ± 0.0080** |
| C2 规则 @8%（对齐 D1） | 192,000 | 0.5311 ± 0.0131 |
| d1_periph @step500 | ~190,000 | 0.5403 ± 0.0030 |
| d1_periph @step300 | ~152,000 | 0.5191 ± 0.0063（vs 匹配随机 **−0.0104**） |

剂量对齐时打平（+0.009 ± 0.013，非配对）。但规则用**少 37% 的边**达到 0.5566，
而 D1 **没法跑得更省**——砍到 152k 就掉到 0.5191 并输给匹配随机。

### Stage 0 — 逐节点的 p 值不值得学（H1 的判决）

| | |
|---|---|
| **目的** | 在建任何学习头之前，先把"p 不该自适应"和"头学不出来"分开。用学习无关的方式测自适应的 headroom |
| **做法** | `stage0_adaptive_p.py`。`p_u = clip(65 + a·zscore(feature_u), 2, 98)`，只扫一个斜率 `a`。`a=0` 精确等于 C2 的全局规则，所以三条曲线过同一基准点。特征：`knn_radius`（第 10 近邻距离）、`centroid`（外围性——C1b 独立标定过的那个量）、`lid`（Levina-Bickel 局部内在维度） |
| **决定性对照** | **把特征在节点间打乱**。`p_u` 的多重集逐值保留（分布、方差、偏度全同），只销毁"哪个节点拿哪个 p" |

| 特征 | 打乱后 Δ（离散度） | real − 打乱（自适应） | 正向 |
|---|---|---|---|
| knn_radius | +0.0070 | +0.0078 | 3/4 |
| centroid | +0.0091 | **−0.0474** | 0/4 |
| lid | +0.0159 | **−0.0010** | 3/4 |

~~**离散度本身：+0.0107 ± 0.0025（11/12 格为正）**~~
↳ **更正：这是共享基线的伪影，见下方"离散度扫描"。**
自适应净效应：lid 归零、knn_radius 未分辨（+0.008 ± 0.008）、centroid **显著为负**
——按真实质心距离对齐 p，比把同一堆 p 值随机发下去还差 0.047。

**分析（已更正）**：自适应没用，离散度也没用（后者是伪影）。按事先写好的判据，
**逐节点 p 的学习头不被支持**。

注意这**不触及 C1**：C1 说的是"给定长度，选哪个点"（+0.014），Stage 0 说的是"选多长"。
两个是不同的决策，结论可以并存。

**适用范围的诚实边界**：只测了 3 个闭式特征 × 线性映射。吃原始 `z_u` 的头理论上可能抓到
这三个标量表达不了的东西——只是目前**没有任何正面证据**，而唯一有独立先验支持的特征
（centroid）测出来是负的。

**过程中被自己推翻的两条推断**（都是我在数据逐行到达时下的，两条都错）：

1. ~~"|a|=15 的格子方差恒等，结果却天差地别 ⇒ 排除方差解释"~~
   ↳ **更正**：方差解释成立。centroid 的负向对齐把它自己的离散度增益吃掉还倒欠，
   伪装成"不像方差效应"。**只有打乱对照能分开这两者。**
2. ~~"centroid 单调递增 ⇒ 源点越外围 p 该越大"~~
   ↳ **更正**：那是 4 个点里取 3 个看出的假趋势。`a=+15` 塌到 −0.1231（尾部被裁到 p=98，
   正好撞进 C1b 标定的死胡同离群点），而打乱对照显示斜率本来就是负的。

**功效不足（新增第 10 条方法论教训 → §3.10）**：两个名义等价的"钉在 65"配置测出
0.5193 和 0.5379。3 draw 的噪声是 ±0.02，所以 **Stage 0 表里任何单元格的绝对值都不可引用**。
配对对比（real − shuffled）能活下来，是因为两次跑共享 slot 和 jitter 向量。

### Stage 0 后续 · 离散度扫描 —— 推翻了 Stage 0 的离散度结论

| | |
|---|---|
| **目的** | 定"p 的离散度"值多少，因为它会改动免学习 bar |
| **做法** | `c2_spread.py`：`p_u ~ clip(N(65, σ), 2, 98)`，σ∈{0.5,15,25}，5 draw × 3 slot seed，slot 内配对 |

| σ | slot11 | slot23 | slot37 | mean | vs σ=0.5 |
|---|---|---|---|---|---|
| 0.5（= C2 的 δ 规则） | 0.5314 | 0.5626 | 0.5619 | **0.5520 ± 0.0103** | — |
| 15 | 0.5323 | 0.5454 | 0.5644 | 0.5474 ± 0.0093 | −0.0046 ± 0.0063（2/3） |
| 25 | 0.5099 | 0.5447 | 0.5483 | 0.5343 ± 0.0122 | **−0.0177 ± 0.0023（0/3）** |

**摊开 p 分布在 σ=15 无效、σ=25 有害。免学习 bar 不变，仍是 δ-at-65。**
σ=0.5 的 0.5520 ± 0.0103 独立复现了 C2 的 0.5566 ± 0.0080。

**为什么和 Stage 0 的 +0.0107 矛盾 —— 共享基线伪影（→ §3.11）**：
12 个打乱格子的 Δ 全部对**同一个** baseline 数相减，而那个 baseline 本身是一次 3-draw
测量（0.5193，事后看抽低了）。基线低 0.01，12 个 Δ 同时被抬高 0.01；把它们当独立样本
算出的 ±0.0025 是假的。"11/12 为正"测的是那一次基线抽样，不是离散度。

**但自适应的结论不受影响**：`real − shuffled` 两边都是实测、共享 slot 和 jitter，
相减把基线整个消掉。所以 Stage 0 的判决只是**更彻底**了：自适应和离散度都没用。

### Stage 1 (D2) — 学习逐节点的 p_u ← **第二个正结果，且推翻了 Stage 0 的适用范围**

| | |
|---|---|
| **目的** | Stage 0 的 null 只对"三个闭式特征 × 线性映射"成立。学习头吃原始 `z_u`，能表达它们表达不了的关系——这是那条反驳的直接检验 |
| **做法** | `lib/agent.py: len_head`（2 层 MLP，输出层零初始化）→ `p_u = 65 + 25·tanh(δ_u)`，高斯采样后 log-prob 接进 PPO ratio 两侧。候选带用**逐节点 rank**（C2 验证过的定义），rank 由该行自己的均匀候选样本估计 |
| **对照臂** | `--len_fixed 65` 走同一条 rank 带代码路径，只是不学。**不能拿 D1 当对照**——那会混进"带定义从全局 CDF 换成逐节点 rank"这一项 |
| **验收** | `stage1_eval.py`，三条全过才算赢，汇总 9 个格子（3 agent × 3 slot seed，格内配对） |

| 检验 | 结果 | t | 判定 |
|---|---|---|---|
| **TEST 2** learned vs **自己的打乱版** | **+0.0321 ± 0.0093（7/9）** | **3.5** | **PASS** |
| TEST 1 learned vs p=65 | +0.0039 ± 0.0026（7/9） | 1.5 | 未分辨 |
| （诊断）打乱版 vs p=65 = 离散度税 | −0.0282 ± 0.0086（2/9） | −3.3 | — |

`p=65 规则 0.5461 ± 0.0070 | learned 0.5500 ± 0.0076 | shuffled 0.5179 ± 0.0132`

**分析**：打乱版持有**完全相同**的 p 多重集，所以 +0.032 不是离散度、不是剂量、不是长度分布，
只能是**哪个节点拿哪个分位**。这是本项目第一个这个量级的、不条件于函数形式的正结果。

**TEST 3 的相关性推翻了 Stage 0 的适用范围**：`centroid` **−0.43 / −0.68 / −0.63**，
knn_radius 和 lid ≈ 0。而 Stage 0 测出**线性**对齐 centroid 是 **−0.047**（比随机分配还差）。
同一个特征，线性用是负的、非线性用是正的 ⇒ **限制在函数形式，不在特征选择**。
Stage 0 的结论从此必须读作"条件于线性"。

**但它没赢规则，账很具体**：span=25 时头把 p_u 推到 sd 19–21，tanh 两端都饱和
（p5≈40、p95≈89），而 p≈90 正是 C2 测出的坏区（0.5126）、C1b 解释过的死胡同离群点。

```
分配带来的信息      +0.032
自选离散度的代价    −0.028
────────────────────────
净赚                +0.004  （t=1.5，未分辨）
```

端到端 0.5456 ± 0.0078（**历史最佳学习臂**；seed 42 的 0.5574 是首次有单个 run 超过 bar 均值 0.5520），
但三 seed 均值仍未分辨。非设计的副作用：ind_max 掉到 542–990，而固定 p 对照是 1702–7401。

**引出 Stage 1b**：约束的应该是**动作空间**而不是网络。`--len_span 10` 把 p_u 限制在 [55, 75]，
按 σ 扫描该处的税约 −0.003。`--len_sigma` 必须同步缩小，否则光探索噪声就能重造出那个昂贵的离散度。
正在跑 `d2b_span{10,15}`。

---

## 6. 现在的机制链条

每一环都有对照，且互相自洽：

```
scorer = dot(MLP(z_u), MLP(z_v))
        │
        │  平滑 encoder ⇒ 邻近输入映射到邻近输出
        ▼
【B1】短边偏好是架构性的（随机初始化 −0.80 比预训练 −0.58 更偏短）
        │  ⇒ 改权重无解：换 loss / 换监督 / 换 encoder 全部无效
        ▼
【B0/A2】policy 花编辑去把长边替换成局部边（长边存活率 15% vs 87%）
        │  ⇒ 从无长边的 kNN 出发几乎无害（+0.017）
        │  ⇒ 从优势就是长边的重连图出发是毁灭性的（−0.158）
        │  ⇒ 两个方向收敛到同一个坏吸引子 ~0.33
        ▼
【Tier0】真正排序 recall 的是预算内 coverage（rho=+1.0），长边直接买 coverage
        ▼
【C2】长度最优在内部，分位 ~65；kNN 是 7.6（太短），policy 枢纽边 93.2（太长）
        ▼
【B2/D1】限制动作空间可以强行把 add_pct 钉到带内 ⇒ 0.299 → 0.502 → 0.540
        ▼
【C1】固定长度后，policy 的目标选择确实有信息（−0.0141 vs 安慰剂 −0.0009）
        │  但只在普通长边上；枢纽边上反过来（+0.025）
        ▼
【C1b】枢纽的害处是那 10 个点的身份（质心距离分位 98.8–99.8 的外围离群点），
        不是入度（逐点精确匹配后 null），不是分配（置换 null）
```

**已彻底关闭的方向**：入度刹车（3 次 null）、换 scorer 权重（架构性）、
更多编辑量（A4）、其它操作点（A3，12/12）、枢纽抑制（2 次 null）。

---

## 7. 下一步猜想（按可证伪性排序）

### ~~H1 · 规则的最优分位是全局的，policy 的价值应该在分布非均匀处~~ **已判决：否**
↳ Stage 0（§5）用打乱对照判掉了：收益全部来自 **p 的离散度**（+0.0107 ± 0.0025，11/12），
**自适应净效应为零**（lid −0.001，knn_radius +0.008 ± 0.008 未分辨），
而 centroid 方向**为负**（−0.047，比随机分配同一堆 p 值还差）。
残留的开口：只测了 3 个闭式特征 × 线性映射，吃原始 `z_u` 的头**理论上**仍可能有别的东西
——但没有正面证据，且唯一有独立先验支持的特征测出来是负的。

原文保留如下备查：

### ~~H1 原文~~
**猜想**：C2 用单一全局 p 就拿到 0.5566，是因为 SIFT100K 局部内在维度/密度足够均匀。
如果数据分布不均匀，单一 p 不再最优，而 policy 能逐节点自适应——这正是 C1 证明它会做的事。

**预测**：按局部内在维度（或 kNN-半径）把顶点分层，各层的最优 p 会显著不同；
且分层最优的 oracle 规则会明显超过单一全局 p。

**判据**：如果各层最优 p 差 <5 个分位点 → H1 死，policy 没有立足空间，转 H4。
如果差 >15 个分位点 → 分层 oracle 给出了 policy 的可达上界，D2 有意义。

**成本**：纯测量，不用训练，~30 分钟。`probe_lenband.py` 可扩展。

### ~~H1' · 离散度才是免费的那一截~~ **已判决：否**
↳ σ 扫描（5 draw × 3 slot seed）：σ=15 是 −0.0046 ± 0.0063，σ=25 是 −0.0177 ± 0.0023（0/3）。
Stage 0 的 +0.0107 是共享基线伪影。**bar 不变，仍是 δ-at-65 的 0.5520–0.5566。**

### H2 · policy 的编辑效率低是因为**没有停止信号**
**猜想**：D1 在 152k 边时是 0.5191，在 190k 边时是 0.5403，而规则在 120k 边就到 0.5566。
policy 需要更多编辑才能达到更差的结果，可能是因为它反复编辑同一批节点
（`accept=off` 移除了逐次编辑的奖励门，冻结策略"不知道何时停手"——这与早期
"no fixed point"的观察一致）。

**预测**：D1 的编辑在节点上高度不均匀；把每节点编辑次数上限设为 1–2 会在更少的边上达到同等 recall。

**判据**：量 D1 的 per-node 编辑次数直方图。若接近均匀 → H2 死。

**成本**：测量 ~20 分钟；若成立，加一个 per-node 编辑配额，1 轮训练。

### H3 · periph 过滤真正的作用是**方差**，而方差本身可能是可发表的
**猜想**：periph 把跨 seed sd 砍了 4.6×（0.0240→0.0052）却不改均值，且不是靠压枢纽。
如果这可复现，"学习方法的**稳定性**优于规则"是一个独立于均值的贡献
——注意 C2 规则自身的跨 slot-seed sd 是 0.0131，**比 D1-periph 差 2.5 倍**。

**预测**：加 seed 到 6 个，D1-periph 的 sd 仍显著低于 C2 规则的 sd。

**判据**：F 检验或 bootstrap 比方差比。

**成本**：3 个额外 seed，~1 小时。

### H4 · 接受负结果，把机制写出来
§6 那条链条本身是完整的、每环有对照的贡献：
**"基于 dot-product scorer 的图编辑 RL 为什么在 ANN 上做不过一行 numpy"**，
并且给出了那一行 numpy 是什么（分位 65 重连，0.5566）。
这是一个有用的实践结论 + 一个清晰的负结果 + 一套可复用的对照方法论（§3）。

**触发条件**：H1 死 且 H2 死。

### 已降优先级
- **B3 换 coverage 奖励**：coverage 与 recall rho=+1.0，换了大概率同构。
  而且现在的瓶颈明确是**动作空间/长度**，不是奖励信号。
- **improve.md 的 NodeFormer 路线**：被 B1 直接否掉（架构性偏好，换 encoder 无效）。

---

## 8. 脚本索引

| 脚本 | 用途 |
|---|---|
| `eval_harness.py` | **唯一评分入口**。多 seed count-matched 对照 + coverage + add_pct |
| `lib_control.py` | 免学习对照（`dose=` / `n_edges=` 两种匹配模式） |
| `probe_visits.py` | coverage / pops / 入度 / recall 的公共测量原语 |
| `ablate_hub_draws.py` | 消融模板（≥5 draw，配对 draw seed） |
| `c1_reshuffle.py` | 等长目标重连（`--exclude_hub` 剔除枢纽边） |
| `c1b_decompose.py` | perm / relabel 分解 |
| `c2_lengthsweep.py` | 长度分位 × 剂量 × slot seed 扫描 |
| `probe_hub_identity.py` | 枢纽的质心距离 / 局部密度 |
| `test_d1_pool.py` | D1 候选池单元检查 |
| `make_control_graph.py` | 把对照图存成 dynamic_edges 快照 |
